{
  "name": "stucktokens",
  "version": "1.0.0",
  "description": "fetchs token balances from covalent index api and calculates possibly stuck token amount",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "KaffinPX",
  "license": "ISC",
  "dependencies": {
    "web3": "^4.0.3"
  }
}
